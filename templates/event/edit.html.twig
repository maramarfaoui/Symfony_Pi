{% extends 'base-back.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
    <h1>Edit Evenement</h1>

    {{ include('event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}<br>

    <a href="{{ path('app_event_index') }}">
        <button class="btn btn-info">Reour en Arrière</button>
    </a><br>

    <br>{{ include('event/_delete_form.html.twig') }}
{% endblock %}
