<form method="post" action="{{ path('app_historiquevente_delete', {'idvent': historiquevente.idvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historiquevente.idvent) }}">
    <button class="btn">Delete</button>
</form>
