<!DOCTYPE html>

<title>New Facture</title>

{% block body %}
    <h1>Create new Facture</h1>

    {{ include('facture/_form.html.twig') }}

    <a href="{{ path('app_facture_index') }}">back to list</a>
{% endblock %}
