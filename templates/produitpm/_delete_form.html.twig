<form method="post" action="{{ path('app_produitpm_delete', {'idprod': produitpm.idprod}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produitpm.idprod) }}">
    <button class="btn">Delete</button>
</form>
