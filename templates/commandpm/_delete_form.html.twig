<form method="post" action="{{ path('app_commandpm_delete', {'idcpm': commandpm.idcpm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandpm.idcpm) }}">
    <button class="btn btn-primary">Delete</button>
</form>
